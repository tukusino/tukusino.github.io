# 開発・ローカル確認ルール

このプロジェクトでは、住民が使う画面を壊さないことと、公開前に実際のスマートフォン画面で確認することを優先します。

## 画面を書き換える処理

- `MutationObserver` などで画面の変化を監視する場合、監視処理自身が行った変更を再び処理しないようにする。
- 監視中にDOMを変更する必要がある場合は、変更中だけ監視を停止し、`finally` で必ず監視を再開する。
- 同じ要素を何度も追加しない。追加前に既存要素を確認する。
- イベントリスナーは、同じ要素へ重複登録しない。
- `setInterval`、`MutationObserver`、再描画処理を追加したときは、無限ループや画面の急激な重さがないか確認する。

## 変更後の必須確認

1. `npm.cmd run lint` を実行する。
2. `npm.cmd run build` を実行する。
3. `npm.cmd run dev:local` でローカルサーバーを起動する。
4. `http://127.0.0.1:5173/` を開く。
5. スマートフォン幅で、トップページが表示されることを確認する。
6. 表示直後だけでなく、2～3秒待っても画面が落ちないことを確認する。
7. お知らせ、行事、ゴミの日の主要画面を確認する。
8. エラー表示や白画面がないことを確認してから、次の作業へ進む。

## 公開に関するルール

- ユーザーから明確な指示があるまで、GitHub Pagesへの公開・デプロイ・pushは行わない。
- ローカル確認が終わっていない状態で公開しない。
- 公開前には、変更内容、確認結果、公開対象を短く報告する。

## ローカル起動に関するルール

- 開発用ポートは `5173` に固定する。
- 起動は `scripts/start-local.ps1`、停止は `scripts/stop-local.ps1` を使う。
- 古いタブが「This page crashed」になった場合は、そのタブを再利用せず、新しいタブで確認する。
- URLが開けたことだけで完了とせず、画面の内容と数秒後の安定表示を確認する。
